<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- Dedicated theme for UCrop to avoid content under status bar/notch -->
    <style name="AppTheme.UCrop" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Ensure we don't draw under the status bar -->
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">never</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="colorAccent">@android:color/white</item>
        <item name="colorPrimary">@android:color/black</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <!-- Make the UCrop toolbar taller globally -->
        <item name="actionBarSize">88dp</item>
        <!-- Increase Toolbar height and top padding inside UCrop -->
        <item name="toolbarStyle">@style/UCropToolbarStyle</item>
    </style>

    <!-- Custom Toolbar style to create visual separation from the status bar -->
    <style name="UCropToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <!-- Make toolbar even taller to provide more space under status bar -->
        <item name="android:minHeight">88dp</item>
        <!-- Add more top margin for title/icons inside the toolbar -->
        <item name="titleMarginTop">16dp</item>
        <!-- Increase top padding for extra spacing -->
        <item name="android:paddingTop">16dp</item>
        <!-- Slightly larger start inset to avoid crowding near the edge -->
        <item name="contentInsetStartWithNavigation">20dp</item>
    </style>
</resources>
